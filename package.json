{
  "name": "knowledge-hub-monorepo",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix automation",
    "dev": "npm run dev --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "test:automation": "node automation/index.js"
  }
}
